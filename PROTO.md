# 🧭 Лекція: Classes, Prototypes, `this`, `super()` і приватні поля у JavaScript

## 🎯 Мета

- Зрозуміти, як працюють **класи** в JavaScript.
- Навчитися використовувати **прототипи** для розширення функціональності.
- Розібратись у поведінці **ключового слова `this`** у класах.
- Зрозуміти роль **`super()`** у наслідуванні.
- Глибоко освоїти **приватні поля і методи** (`#field`, `#method`).
- Навчитись створювати **статичні методи** і розширювати **вбудовані прототипи**.

---

## 🏗️ 1. Класи в JavaScript

Класи — це *синтаксичний цукор* над прототипно-орієнтованою моделлю JavaScript.
Вони забезпечують більш зрозумілу та структуровану роботу з об'єктами.

```js
class User {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  sayHello() {
    console.log(`Hello, my name is ${this.name}`);
  }
}

const user = new User("Alice", 25);
user.sayHello(); // Hello, my name is Alice
```

---

## ⚙️ 2. Прототипи

Кожен об’єкт у JavaScript має внутрішнє посилання на свій **прототип** (`[[Prototype]]`).

```js
function Person(name) {
  this.name = name;
}

Person.prototype.sayHello = function () {
  console.log(`Hi, I'm ${this.name}`);
};

const person = new Person("Bob");
person.sayHello(); // Hi, I'm Bob
```

> **Пам’ятайте:** класи — це просто зручна надбудова над цією прототипною системою.

---

## 🧬 3. Наслідування класів і ключове слово `super`

JavaScript дозволяє створювати нові класи на основі старих за допомогою `extends`, а `super` використовується для виклику батьківського конструктора або методів.

### 🔹 3.1. Виклик `super()` у конструкторі

`super()` викликає **конструктор батьківського класу** і ініціалізує `this`.

```js
class Animal {
  constructor(name) {
    this.name = name;
    console.log("Animal created");
  }
}

class Dog extends Animal {
  constructor(name, breed) {
    super(name); // викликає Animal.constructor(name)
    this.breed = breed;
  }
}
```

Без `super()`, не можна використовувати `this` у конструкторі підкласу — це спричинить помилку.

---

### 🔹 3.2. Виклик методів із `super.method()`

У перевизначених методах можна викликати батьківські методи через `super.methodName()`:

```js
class Animal {
  speak() {
    console.log("The animal makes a sound");
  }
}

class Dog extends Animal {
  speak() {
    super.speak(); // виклик методу базового класу
    console.log("Woof!");
  }
}
```

---

### 🔹 3.3. `super` у статичних методах

```js
class Parent {
  static greet() {
    console.log("Hello from Parent");
  }
}

class Child extends Parent {
  static greet() {
    super.greet();
    console.log("Hello from Child");
  }
}

Child.greet();
// Hello from Parent
// Hello from Child
```

---

## 🧭 4. Ключове слово `this`

`this` — це контекст виклику методу, який часто вказує на екземпляр класу.

```js
class Counter {
  constructor() {
    this.value = 0;
  }

  increment() {
    this.value++;
    console.log(this.value);
  }
}
```

> Якщо передати метод у callback без bind — `this` втрачається.

---

## 🔐 5. Приватні поля та методи

### 🔹 5.1. Що це таке?

JavaScript підтримує **справжню інкапсуляцію** завдяки **приватним полям**, які починаються з символу `#`.

```js
class Account {
  #balance = 0; // приватне поле

  deposit(amount) {
    this.#balance += amount;
  }

  getBalance() {
    return this.#balance;
  }
}

const acc = new Account();
acc.deposit(100);
console.log(acc.getBalance()); // ✅ 100
console.log(acc.#balance); // ❌ SyntaxError: Private field '#balance' must be declared in an enclosing class
```

---

### 🔹 5.2. Основна ідея

До появи `#`, розробники імітували “приватність” через підкреслення або замикання:

```js
this._balance = 0; // умовно "приватне" поле
```

Але це лише **угода між програмістами** — доступ до `_balance` можна отримати без проблем.
Приватні поля `#balance` є **по-справжньому приватними**, і до них не можна отримати доступ навіть через `this["#balance"]`.

---

### 🔹 5.3. Приватні методи

Ви можете приховати логіку за допомогою приватних методів, як у повноцінному ООП:

```js
class User {
  #validateName(name) {
    if (typeof name !== "string" || name.length < 2)
      throw new Error("Invalid name");
  }

  setName(name) {
    this.#validateName(name); // ✅ доступний всередині класу
    this.name = name;
  }
}

const u = new User();
u.setName("Max"); // працює
// u.#validateName("x"); ❌ SyntaxError
```

---

### 🔹 5.4. Властивості приватних полів

| Властивість | Опис |
|--------------|------|
| Починаються з `#` | синтаксичне правило мови |
| Не видно поза класом | навіть через Reflect або Object.keys |
| Не наслідуються | у підкласі потрібно оголошувати заново |
| Не динамічні | не можна звернутись через `this["#field"]` |
| Забезпечують справжню безпечну інкапсуляцію | захищають дані на рівні мови |
| Не доступні через `prototype` | не є частиною прототипного ланцюга |

---

### 🔹 5.5. Приватні поля та наслідування

Приватні поля **не передаються у спадок**.
Кожен клас має свої власні приватні поля — підклас не може отримати доступ до `#field` базового класу.

```js
class Parent {
  #secret = "hidden";
  reveal() {
    console.log(this.#secret);
  }
}

class Child extends Parent {
  showParentSecret() {
    console.log(this.#secret); // ❌ Error
  }
}
```

Якщо треба "спільна приватність", можна використовувати **protected-патерн** через `_назву` (домовленість).

---

### 🔹 5.6. Поєднання з геттерами / сеттерами

```js
class Temperature {
  #celsius = 0;

  get fahrenheit() {
    return this.#celsius * 1.8 + 32;
  }

  set fahrenheit(f) {
    this.#celsius = (f - 32) / 1.8;
  }
}

const t = new Temperature();
t.fahrenheit = 100;
console.log(t.fahrenheit); // 100
```

---

### 🔹 5.7. Коротке порівняння

| Тип поля | Можна змінити ззовні? | Вважається приватним? | Доступ з підкласу |
|-----------|----------------------|------------------------|------------------|
| `this.field` | ✅ Так | ❌ Ні | ✅ Так |
| `this._field` | ✅ Так | ⚠ “домовленість” | ✅ Так |
| `this.#field` | ❌ Ні | ✅ Так (справжнє) | ❌ Ні |

---

## 🧰 6. Зв’язок між класами та прототипами

```js
console.log(User.prototype.__proto__ === Object.prototype); // true
```

![Prototype chain](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Objects/Object_prototypes/js-prototype-chain.svg)

---

## ⚡ 7. Статичні методи

Статичні методи прив’язані до **класу**, а не до його екземплярів.

```js
class MathUtils {
  static add(a, b) {
    return a + b;
  }
}

console.log(MathUtils.add(2, 3)); // 5
```

---

## 🧮 8. Розширення вбудованих прототипів

```js
Array.prototype.sum = function () {
  return this.reduce((total, num) => total + num, 0);
};

console.log([1, 2, 3, 4].sum()); // 10
```

> 🧠 Будьте обережні: зміна вбудованих прототипів може викликати конфлікти.

---

## 📚 9. Підсумок

| Тема | Основна ідея |
|------|---------------|
| **Class** | Синтаксична надбудова над прототипами |
| **Prototype** | Механізм наслідування властивостей |
| **this** | Поточний об’єкт у контексті |
| **extends / super()** | Наслідування і виклик базового класу |
| **#privateField** | Справжня інкапсуляція, недоступна поза класом |
| **static method** | Доступний лише для самого класу |
| **Array.prototype.sum()** | Розширення вбудованих типів |

---

## 🧩 10. Завдання для практики

1. Створіть клас `Student` з властивістю `name` і методом `displayInfo()`.
2. Додайте підклас `GraduateStudent` з `thesisTitle`, який викликає `super.displayInfo()`.
3. Реалізуйте приватне поле `#visits` і метод `incrementVisits()`.
4. Додайте до класу `Student` **геттер/сеттер** для `fullName`.
5. Реалізуйте статичний метод `compareGrades(a, b)`.
6. Створіть власний метод `avg()` для `Array.prototype`.
7. Реалізуйте приватний метод `#formatInfo()` у `GraduateStudent`, який використовується лише всередині класу.

---

## 📎 Ресурси

- [MDN: Classes](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes)
- [MDN: Private class fields](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/Private_class_fields)
- [MDN: Private methods](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/Private_class_methods)
- [JavaScript.info – Class Fields](https://javascript.info/private-protected-properties-methods)
- [MDN: Prototypes](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Objects/Object_prototypes)
- [JavaScript.info – Classes](https://javascript.info/class)
