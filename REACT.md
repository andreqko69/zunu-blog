# 💡 Лекція: Вступ до React

> **Мета:** Розібратись, що таке React, як він працює, для чого його використовують та написати перший компонент.

---

## 🧩 Зміст

1. [Що таке React](#-що-таке-react)
2. [Чому React](#-чому-react)
3. [Ключові концепції](#-ключові-концепції)
   - Компоненти
   - JSX
   - Props
   - State
4. [Функціональні компоненти та Hooks](#-функціональні-компоненти-та-hooks)
5. [Virtual DOM](#-virtual-dom)
6. [Життєвий цикл компонента](#-життєвий-цикл-компонента)
7. [Перший React компонент](#-перший-react-компонент)
8. [Висновки](#-висновки)

---

## 🔹 Що таке React

**React** — це JavaScript-бібліотека для створення користувацьких інтерфейсів (UI) за допомогою **компонентного підходу**.

```js
import React from 'react';

function App() {
  return <h1>Привіт, React!</h1>;
}

export default App;
```

- React розроблена **Facebook** (тепер Meta)
- Дозволяє **декларативно** описувати інтерфейс
- Базується на **компонентах** — переважних блоках інтерфейсу

---

## 🔹 Чому React

### 1. **Компонентний підхід**
Розділяє UI на невеликі, переповторні блоки.

### 2. **Virtual DOM**
React відстежує змінення і оновлює тільки необхідні частини DOM.

### 3. **Однонаправлений потік даних**
Легше відстежувати та передбачити поведінку програми.

### 4. **Велика екосистема**
Безліч бібліотек та інструментів (Redux, Router, UI Framework та ін.).

### 5. **Популярність та спільнота**
Величезна кількість ресурсів, туторіалів та пакетів.

---

## 🔹 Ключові концепції

### 1. Компоненти

Компонент — це функція або клас, що повертає JSX.

```js
function Welcome() {
  return <p>Добро пожалувати!</p>;
}

// Використання
<Welcome />
```

---

### 2. JSX

**JSX** — це розширення синтаксису JavaScript, яке виглядає як HTML, але це все ще JavaScript.

```js
const greeting = <h1>Привіт, {name}!</h1>;

// Компілюється в:
const greeting = React.createElement('h1', null, `Привіт, ${name}!`);
```

✅ Робить код читабельнішим
✅ Дозволяє змішувати HTML та JavaScript

---

### 3. Props

**Props** (властивості) — це спосіб передавання даних від батька до дитини.

```js
function Greeting(props) {
  return <h1>Привіт, {props.name}!</h1>;
}

// Або з деструктуризацією
function Greeting({ name, age }) {
  return <div>
    <h1>Привіт, {name}!</h1>
    <p>Вік: {age}</p>
  </div>;
}

// Використання
<Greeting name="Олег" age={25} />
```

🧠 Props **не можна змінювати** всередині компонента (read-only).

---

### 4. State

**State** — це дані, які можна змінювати всередині компонента.

```js
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Лічильник: {count}</p>
      <button onClick={() => setCount(count + 1)}>
        Збільшити
      </button>
    </div>
  );
}
```

- `useState` — Hook для управління state
- `count` — поточне значення
- `setCount` — функція для оновлення

---

## 🔹 Функціональні компоненти та Hooks

### Функціональні компоненти (сучасний підхід)

```js
function MyComponent() {
  return <div>Функціональний компонент</div>;
}
```

### Hooks

**Hooks** — це функції, що дозволяють використовувати state та інші можливості в функціональних компонентах.

```js
import { useState, useEffect } from 'react';

function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    // Завантажити дані користувача
    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(data => setUser(data));
  }, [userId]); // Залежності

  if (!user) return <p>Завантаження...</p>;

  return (
    <div>
      <h1>{user.name}</h1>
      <p>{user.email}</p>
    </div>
  );
}
```

#### Популярні Hooks:
- `useState` — управління state
- `useEffect` — побічні ефекти (API запити, підписки)
- `useContext` — доступ до контексту
- `useRef` — пряме посилання на DOM
- `useReducer` — складне управління state

---

## 🔹 Virtual DOM

**Virtual DOM** — це абстрактне представлення реального DOM в пам'яті React.

### Як це працює:

1. Ви змінюєте state → компонент перерендерується
2. React створює новий Virtual DOM
3. Порівнює його з попередньою версією
4. Знаходить різниці ("diffing")
5. Оновлює тільки ці частини у реальному DOM

```
State змінено ↓
┌─────────────────────┐
│ Новий Virtual DOM   │
└─────────────────────┘
         ↓ порівняння
┌─────────────────────┐
│ Старий Virtual DOM  │
└─────────────────────┘
         ↓ diffing
┌─────────────────────┐
│ Оновити тільки ці   │
│ елементи у DOM      │
└─────────────────────┘
```

✅ Швидше, ніж оновлювати весь реальний DOM

---

## 🔹 Життєвий цикл компонента

### У функціональних компонентах (Hooks):

```js
function Example() {
  // Мантує
  useEffect(() => {
    console.log("Компонент змонтований");
    
    // Очистка
    return () => {
      console.log("Компонент розмонтований");
    };
  }, []); // Залежності

  // Оновлення
  useEffect(() => {
    console.log("Компонент оновився");
  }, [dependency]); // Запускається при зміні dependency

  return <div>Приклад</div>;
}
```

#### Етапи життя:
1. **Монтування** — компонент додається до DOM
2. **Оновлення** — компонент перерендерується при зміні props або state
3. **Розмонтування** — компонент видаляється з DOM

---

## 🔹 Перший React компонент

### Простий компонент Todo App:

```js
import React, { useState } from 'react';

function TodoApp() {
  const [todos, setTodos] = useState([]);
  const [input, setInput] = useState('');

  const addTodo = () => {
    if (input.trim()) {
      setTodos([...todos, { id: Date.now(), text: input, done: false }]);
      setInput('');
    }
  };

  const toggleTodo = (id) => {
    setTodos(todos.map(todo =>
      todo.id === id ? { ...todo, done: !todo.done } : todo
    ));
  };

  const deleteTodo = (id) => {
    setTodos(todos.filter(todo => todo.id !== id));
  };

  return (
    <div style={{ maxWidth: '500px', margin: '50px auto' }}>
      <h1>Мої справи</h1>
      
      <div>
        <input
          type="text"
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyPress={(e) => e.key === 'Enter' && addTodo()}
          placeholder="Введіть нову справу..."
        />
        <button onClick={addTodo}>Додати</button>
      </div>

      <ul>
        {todos.map(todo => (
          <li key={todo.id}>
            <input
              type="checkbox"
              checked={todo.done}
              onChange={() => toggleTodo(todo.id)}
            />
            <span style={{ textDecoration: todo.done ? 'line-through' : 'none' }}>
              {todo.text}
            </span>
            <button onClick={() => deleteTodo(todo.id)}>Видалити</button>
          </li>
        ))}
      </ul>
    </div>
  );
}

export default TodoApp;
```

---

## 🧠 Висновки

- ✅ React — це бібліотека для створення UI через компоненти
- ✅ Компоненти можуть приймати **Props** та мати **State**
- ✅ JSX дозволяє писати HTML-подібний код у JavaScript
- ✅ Virtual DOM оптимізує оновлення інтерфейсу
- ✅ Hooks (useState, useEffect) спрощують управління компонентами
- ✅ React розвиває декларативний підхід до побудови UI

---

📘 **Рекомендовано почитати:**
- [React Official Docs](https://react.dev)
- [MDN — React Documentation](https://developer.mozilla.org/en-US/docs/Glossary/React)
- [React Hooks API Reference](https://react.dev/reference/react/hooks)