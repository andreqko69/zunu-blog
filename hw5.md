# Домашнє завдання: Scope, Closure та Date у JavaScript

---

### 🧩 Завдання 1: Лічильник кроків

Створіть функцію `createStepCounter()`, яка повертає об’єкт з двома методами:
- `step()` — збільшує кількість кроків на 1;
- `getSteps()` — повертає поточну кількість кроків.

Кількість кроків має зберігатися **у замиканні** (тобто не бути доступною напряму).

---

### 🧩 Завдання 2: Банківський рахунок

Створіть функцію `createBankAccount(initialBalance)`, яка приймає початковий баланс і повертає об’єкт з методами:

- `deposit(amount)` — додає кошти;
- `withdraw(amount)` — знімає кошти (з перевіркою на наявність достатнього балансу);
- `getBalance()` — повертає поточний баланс.

> 💡 Баланс повинен бути приватним (доступ лише через методи функції).

---

### 🧩 Завдання 3: Функція зберігає ім’я користувача

Створіть функцію `createGreeter(name)`, яка приймає ім’я користувача і повертає **іншу функцію**, що при виклику виводить у консоль привітання:

```js
const greetOlya = createGreeter("Оля");
greetOlya(); // "Привіт, Оля!"

const greetMax = createGreeter("Максим");
greetMax(); // "Привіт, Максим!"
```

> 💡 Пояснення: функція `createGreeter` має «пам’ятати» ім’я, передане під час створення, навіть після того, як вона вже виконалася.
> Це завдання демонструє, як **замикання** дозволяє зберігати доступ до змінних у лексичній області.

---

### 🧩 Завдання 4: Затриманий лічильник

Створіть цикл від 1 до 5, який виводить значення `i` у консоль через 1 секунду *після кожної ітерації*.
Реалізуйте це так, щоб у консолі послідовно з’явилися числа:
