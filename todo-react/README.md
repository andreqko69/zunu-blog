# 📝 Todo App - React

Функціональний todo додаток, створений з використанням React та сучасних можливостей (Hooks).

## 🎯 Функціональність

- ✅ Додавання нових справ
- ✅ Позначення справ як виконані/невиконані
- ✅ Видалення справ
- ✅ Фільтрація (Всі, Активні, Виконані)
- ✅ Статистика (Всього, Активних, Виконаних)
- ✅ Зберігання в localStorage
- ✅ Компонентна архітектура
- ✅ Адаптивний дизайн
- ✅ Підтримка темного режиму

## 📂 Структура файлів

```
todo-react/
├── public/
│   └── index.html          # HTML шаблон
├── src/
│   ├── components/
│   │   ├── TodoInput.jsx   # Компонент введення
│   │   ├── TodoList.jsx    # Компонент списку
│   │   ├── TodoItem.jsx    # Компонент одного todo
│   │   ├── FilterButtons.jsx # Компонент фільтрів
│   │   ├── TodoStats.jsx   # Компонент статистики
│   │   └── TodoActions.jsx # Компонент дій
│   ├── App.jsx             # Головний компонент
│   ├── App.css             # Стилі додатку
│   ├── index.js            # Точка входу
│   └── index.css           # Глобальні стилі
├── package.json            # Залежності проекту
└── README.md               # Цей файл
```

## 🚀 Як запустити

### Передумови

- Node.js (v14 або вище)
- npm або yarn

### Встановлення та запуск

```bash
# Встановіть залежності
npm install

# Запустіть розробницький сервер
npm start

# Сторінка автоматично відкриється на http://localhost:3000
```

### Окремо: Збірка для продакшену

```bash
npm run build
# Створить оптимізовану версію у папці `build/`
```

## 🏗️ Архітектура

### Компоненти

Додаток складається з кількох переїжджаючих компонентів:

1. **App.jsx** - Головний компонент, що управляє станом
2. **TodoInput.jsx** - Форма для введення нових справ
3. **TodoList.jsx** - Список todos
4. **TodoItem.jsx** - Окремий todo елемент
5. **FilterButtons.jsx** - Кнопки фільтрації
6. **TodoStats.jsx** - Відображення статистики
7. **TodoActions.jsx** - Кнопки дій (очистка, видалення)

### Управління станом

Основний стан управляється в компоненті `App`:

```javascript
const [todos, setTodos] = useState([]);
const [currentFilter, setCurrentFilter] = useState('all');
```

### Hooks

Проект використовує такі React Hooks:

- **useState** - управління станом todos та фільтру
- **useEffect** - завантаження/збереження в localStorage
- **useRef** - фокус на input в TodoInput (опціонально)

## 💾 Зберігання даних

Всі справи автоматично зберігаються в `localStorage`:

```javascript
useEffect(() => {
  localStorage.setItem('todos', JSON.stringify(todos));
}, [todos]);
```

Дані завантажуються при монтуванні компонента та зберігаються на кожну зміну.

## 🎨 Дизайн та стилі

- Адаптивний дизайн для всіх розмірів екранів
- CSS з використанням CSS змінних
- Плавні анімації та переходи
- Автоматична підтримка темного режиму
- Приємна користувальницька інтерфейс

## 🔄 Життєвий цикл

1. **Монтування** - завантажити todos з localStorage
2. **Рендеринг** - показати UI на основі стану
3. **Користувальницька взаємодія** - додавання/видалення/фільтрація
4. **Оновлення** - збереження в localStorage
5. **Розмонтування** - очистка (якщо потрібна)

## 📊 Дані Todo

Кожен todo має структуру:

```javascript
{
  id: 1234567890,           // Унікальний ID (timestamp)
  text: "Вивчити React",    // Текст справи
  completed: false,         // Статус виконання
  createdAt: "01.01.2024"   // Дата створення
}
```

## 🎓 Ключові концепції React

Цей проект демонструє:

- **Компонентна архітектура** - розділення на переїжджаючі компоненти
- **Props** - передача даних від батька до дитини
- **State** - управління станом компонента
- **Hooks** - використання useState, useEffect
- **Event Handling** - обробка подій користувача
- **Conditional Rendering** - умовне відображення
- **Списки та ключі** - рендеринг списків з unique keys
- **localStorage API** - перистентність даних

## 📱 Браузерна сумісність

- Chrome (останні версії)
- Firefox (останні версії)
- Safari (останні версії)
- Edge (останні версії)

## 🚦 Поповнення функціоналу

Можна легко розширити проект:

- Додати редагування справ
- Додати категорії/теги
- Додати різні представлення (календар, таблиця)
- Додати синхронізацію з бекендом
- Додати авторизацію користувачів
- Додати експорт/імпорт даних

## 📚 Ресурси

- [React Документація](https://react.dev)
- [React Hooks API](https://react.dev/reference/react)
- [Create React App](https://create-react-app.dev/)

## 🔍 Порівняння з Vanilla JS версією

| Характеристика | Vanilla JS | React |
|---|---|---|
| Рядків коду | ~200 | ~300 (розділено на компоненти) |
| Управління DOM | Вручну | Автоматично |
| Організація коду | Один клас | Компоненти |
| Масштабованість | Важко | Легко |
| Навчальна крива | Низька | Середня |
| Продуктивність | Хороша | Відмінна |

## 🤝 Рекомендації для розробки

1. Використовуйте React Developer Tools розширення
2. Вивчайте React docs для подальшого розвитку
3. Експериментуйте з додаванням нових функцій
4. Практикуйте розподіл компонентів на менші частини

---

**Створено як навчальний проект для демонстрації React! 🚀**