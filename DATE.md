# 🕒 Лекція: Об'єкт Date у JavaScript

## 🧩 Вступ

Клас `Date` у JavaScript — це вбудований об'єкт, який використовується для роботи з датами та часом.
Він дозволяє створювати, змінювати, форматувати й обчислювати різницю між датами.

---

## 1. 📅 Створення об'єкта Date

Існує кілька способів створити об’єкт `Date`.

```js
// Поточна дата і час
const now = new Date();
console.log(now);

// За рядком дати
const fromString = new Date("2025-10-14T18:00:00");
console.log(fromString);

// За заданими параметрами: (рік, місяць, день, година, хвилина, секунда, мілісекунда)
const customDate = new Date(2025, 9, 14, 18, 0, 0);
console.log(customDate);
// ⚠️ Місяці нумеруються з 0 (тобто 9 — це жовтень)
```

---

## 2. ⏰ Основні методи для отримання часу

Об’єкт `Date` містить безліч методів для зчитування дати й часу.

```js
const date = new Date();

console.log(date.getFullYear()); // Поточний рік
console.log(date.getMonth()); // Поточний місяць (0–11)
console.log(date.getDate()); // Число місяця
console.log(date.getDay()); // День тижня (0 — неділя, 6 — субота)
console.log(date.getHours()); // Години
console.log(date.getMinutes()); // Хвилини
console.log(date.getSeconds()); // Секунди
console.log(date.getMilliseconds()); // Мілісекунди
console.log(date.getTime()); // Мілісекунди з 1 січня 1970 (timestamp)
```

---

## 3. 🧱 Методи зміни дати

Методи, що починаються із `set`, дозволяють змінювати частини дати.

```js
const date = new Date();

date.setFullYear(2030);
date.setMonth(5); // червень (0 = січень)
date.setDate(10);
date.setHours(12);

console.log(date);
```

Так можна змінювати будь-яку складову дати без створення нового об’єкта.

---

## 4. 🧮 Робота з timestamp

**Timestamp** — це кількість мілісекунд, що минули з 1 січня 1970 року UTC.

```js
const now = new Date();
console.log(now.getTime()); // timestamp

const timestamp = Date.now(); // короткий спосіб
console.log(timestamp);
```

Можна створити дату з timestamp:

```js
const dateFromTimestamp = new Date(1730000000000);
console.log(dateFromTimestamp);
```

---

## 5. 🌍 Часові зони та UTC

JavaScript автоматично використовує **локальний часовий пояс**, але також дозволяє отримати значення в UTC.

```js
const date = new Date();

console.log(date.getHours()); // локальний час
console.log(date.getUTCHours()); // UTC години
```

Для перетворень між часовими зонами найкраще використовувати бібліотеки, наприклад **`moment.js`** або **`date-fns-tz`**, бо стандартний об’єкт `Date` має обмежені можливості у цій галузі.

---

## 6. 🧭 Форматування дат

Об’єкт `Date` можна вивести у зручному вигляді за допомогою вбудованих методів або `Intl.DateTimeFormat`.

```js
const date = new Date();

// Стандартні методи
console.log(date.toDateString()); // Наприклад: "Tue Oct 14 2025"
console.log(date.toISOString()); // Формат ISO: "2025-10-14T15:00:00.000Z"
console.log(date.toLocaleString()); // Формат залежить від мови браузера

// Використання Intl.DateTimeFormat
const formatter = new Intl.DateTimeFormat("uk-UA", {
  weekday: "long",
  year: "numeric",
  month: "long",
  day: "numeric",
  hour: "2-digit",
  minute: "2-digit"
});

console.log(formatter.format(date));
// "вівторок, 14 жовтня 2025 р., 18:00"
```

---

## 7. ⏳ Обчислення між датами

Щоб обчислити різницю між двома датами, достатньо відняти одну від іншої.

```js
const start = new Date("2025-10-01");
const end = new Date("2025-10-14");

const diffMs = end - start; // різниця в мілісекундах
const diffDays = diffMs / (1000 * 60 * 60 * 24);

console.log(`Між датами ${diffDays} днів`);
```

---

## 8. 🧩 Приклади практичного використання

### a) Таймер відліку
```js
function countdown(targetDate) {
  const now = new Date();
  const diff = targetDate - now;

  const days = Math.floor(diff / (1000 * 60 * 60 * 24));
  const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
  const minutes = Math.floor((diff / (1000 * 60)) % 60);
  const seconds = Math.floor((diff / 1000) % 60);

  return `${days}д ${hours}г ${minutes}хв ${seconds}с`;
}

console.log(countdown(new Date("2025-12-31T23:59:59")));
```

### b) Перевірка віку користувача
```js
function getAge(birthDate) {
  const now = new Date();
  const diff = now - new Date(birthDate);
  const age = Math.floor(diff / (1000 * 60 * 60 * 24 * 365.25));
  return age;
}

console.log(getAge("2000-10-14"));
```

---

## 🧭 Коротке резюме

| Концепт | Опис |
|----------|------|
| **`new Date()`** | Створює об'єкт поточної дати |
| **`get*` / `set*` методи** | Отримання та зміна частин дати |
| **`Date.now()`** | Поточний timestamp |
| **`toLocaleString()`** | Локалізований текстовий формат |
| **UTC методи** | Доступ до дати/часу за світовим часом |
| **Віднімання дат** | Повертає різницю в мілісекундах |

---

## 🌐 Додатково для вивчення

- MDN: [Date](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date)
- MDN: [Intl.DateTimeFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat)
- MDN: [Using time zones](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/Using_time_zones)

---

## 🧩 Висновок

Об’єкт `Date` — це потужний інструмент для роботи з часом у JavaScript.
Він дозволяє:
- оперувати поточними чи довільними датами,
- обчислювати проміжки часу,
- адаптувати формат під різні локалі.

Однак для складних сценаріїв (таймзони, формати, часові гони) рекомендується використовувати **додаткові бібліотеки** на кшталт `moment.js` або `date-fns`.

---
