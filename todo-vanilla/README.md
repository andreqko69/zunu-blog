# 📝 Todo App - Vanilla JavaScript

Це простий Todo додаток, написаний на чистому JavaScript без будь-яких фреймворків або бібліотек.

## 🚀 Як запустити

1. Відкрийте файл `index.html` у вашому веб-браузері
2. Или запустіть локальний сервер:
   ```bash
   # Python 3
   python -m http.server 8000
   
   # Node.js
   npx http-server
   ```
3. Перейдіть на `http://localhost:8000` (або відповідний URL)

## 📋 Функціональність

- ✅ **Додавання справ** - введіть текст і натисніть "Додати" або Enter
- ✅ **Позначення як виконане** - клікніть на checkbox
- ✅ **Видалення справ** - натисніть кнопку 🗑️
- ✅ **Фільтрування** - фільтруйте за статусом (Всі, Активні, Виконані)
- ✅ **Статистика** - переглядайте кількість справ
- ✅ **Очистка** - видаліть всі виконані справи
- ✅ **Локальне сховище** - всі дані зберігаються у браузері

## 📁 Структура файлів

```
todo-vanilla/
├── index.html      # HTML структура
├── main.js         # Основна логіка додатку (клас TodoApp)
├── styles.css      # Стилі і оформлення
└── README.md       # Цей файл
```

## 🔧 Технологічні деталі

### Архітектура

Додаток використовує **класову архітектуру** з класом `TodoApp`, який керує:
- DOM елементами
- Станом приложення (todos)
- Event слухачами
- Локальним сховищем

### Основні методи класу

| Метод | Описання |
|-------|---------|
| `addTodo()` | Додати новий todo в список |
| `toggleTodo(id)` | Залишити/роззалишити todo |
| `deleteTodo(id)` | Видалити todo з списку |
| `clearCompleted()` | Видалити всі виконані todos |
| `deleteAll()` | Видалити всі todos |
| `setFilter(filter)` | Встановити фільтр |
| `render()` | Перерисувати інтерфейс |
| `saveToLocalStorage()` | Зберегти todos у localStorage |
| `loadFromLocalStorage()` | Завантажити todos з localStorage |

## 🎨 Особливості стилізації

- Адаптивний дизайн (мобільна оптимізація)
- Темна тема (автоматична за системними налаштуваннями)
- Плавні переходи та анімації
- Градієнтне тло

## 💾 Локальне сховище

Всі справи автоматично зберігаються у `localStorage` браузера під ключем `todos`. 
Дані зберігаються у форматі JSON та завантажуються при кожному завантаженні сторінки.

## 🖥️ Підтримані браузери

- Chrome/Chromium (останні версії)
- Firefox (останні версії)
- Safari (останні версії)
- Edge (останні версії)

## 📖 Порівняння з React версією

Цей Vanilla JS додаток демонструє:
- Як управляти DOM вручну (без віртуального DOM)
- Як організувати код без фреймворку
- Базові паттерни управління станом
- Event delegation для оптимізації

Загалом React версія має:
- Більш декларативний підхід
- Компонентну архітектуру
- Легшу масштабованість
- Менше коду для обробки DOM

## 🤝 Сприяння

Будь ласка, не соромтеся пропонувати покращення або повідомляти про баги!

---

**Створено як навчальний проект для демонстрації Vanilla JavaScript.**