# 🧭 Лекція: Context і ключове слово `this` у JavaScript

## 🧩 Вступ

У JavaScript контекст виконання (`execution context`) визначає, **яким є значення `this`** у момент виклику функції.
Розуміння `this` життєво важливе, адже воно змінюється залежно від способу виклику функції.

Крім того, JavaScript має три методи — `bind`, `call` і `apply` — які дозволяють **керувати контекстом вручну**.

---

## 1. ⚙️ Що таке контекст (Context)

**Контекст** — це об’єкт, на який посилається ключове слово `this` під час виконання функції.
Іншими словами, `this` — це "власник" поточного коду.

```js
const user = {
  name: "Оля",
  sayHi() {
    console.log("Привіт, я " + this.name);
  }
};

user.sayHi(); // "Привіт, я Оля"
```

> У цьому випадку контекстом (`this`) є об’єкт `user`.

---

## 2. 💡 Основні правила визначення `this`

| Сценарій | Як визначається `this` |
|-----------|------------------------|
| **Звичайний виклик функції** | `this` = `undefined` (у строгому режимі) або `window` (у браузері без strict mode) |
| **Метод об’єкта** | `this` → об’єкт, через який викликана функція |
| **Конструктор (new)** | `this` → новий створений об’єкт |
| **Явне прив’язування (bind / call / apply)** | `this` → об'єкт, переданий вручну |

---

## 3. 📞 `this` у звичайних функціях і стрілкових

### 3.1. У звичайній функції
```js
function showThis() {
  console.log(this);
}

showThis(); // undefined (у strict mode) або window (без нього)
```

### 3.2. У методі об’єкта
```js
const person = {
  name: "Максим",
  showThis() {
    console.log(this.name);
  }
};

person.showThis(); // "Максим"
```

### 3.3. У стрілкових функціях
Стрілкові функції **не мають власного `this`** — вони беруть контекст із місця, де були оголошені.

```js
const user = {
  name: "Оля",
  showThis: () => {
    console.log(this.name);
  }
};

user.showThis(); // undefined
```

> У прикладі вище `this` не посилається на `user`, бо визначається **лексично** — у місці оголошення функції (глобально).

---

## 4. 🧱 this у конструкторах

```js
function User(name) {
  this.name = name;
}

const u = new User("Іван");
console.log(u.name); // "Іван"
```

При виклику через `new`:
1. Створюється новий порожній об’єкт.
2. `this` прив’язується до нього.
3. Функція виконується, і якщо вона нічого не повертає явно, повертається цей об’єкт.

---

## 5. 🧠 Методи ручного керування контекстом

JavaScript дозволяє **явно вказати**, з яким контекстом (`this`) має виконатися функція.
Для цього існують три спеціальні методи: `call`, `apply`, `bind`.

---

### 5.1. `call()`

- Викликає функцію негайно.
- Перший аргумент — об’єкт, який буде контекстом (`this`).
- Подальші аргументи — звичайні параметри функції.

```js
function greet(greeting, punctuation) {
  console.log(`${greeting}, ${this.name}${punctuation}`);
}

const user = { name: "Оля" };

greet.call(user, "Привіт", "!"); // "Привіт, Оля!"
```

---

### 5.2. `apply()`

- Працює аналогічно до `call()`, але **параметри передаються масивом**.

```js
function greet(greeting, punctuation) {
  console.log(`${greeting}, ${this.name}${punctuation}`);
}

const user = { name: "Максим" };

greet.apply(user, ["Вітаю", "!!!"]); // "Вітаю, Максим!!!"
```

---

### 5.3. `bind()`

- **Не викликає** функцію відразу.
- Створює **нову функцію**, у якій зафіксовано контекст.

```js
function greet() {
  console.log("Привіт, " + this.name);
}

const user = { name: "Оксана" };

const greetOksana = greet.bind(user);

greetOksana(); // "Привіт, Оксана"
```

> `bind` часто використовують у колбеках або таймерах, щоб зберегти правильний `this`.

---

## 6. ⏳ Приклад із втратою контексту

```js
const user = {
  name: "Марія",
  sayHi() {
    console.log("Привіт, " + this.name);
  }
};

setTimeout(user.sayHi, 1000); // ❌ "Привіт, undefined"
```

> У цьому випадку `this` втрачається, бо функція передається **як посилання**, а не викликається через об’єкт.

📍 Вирішення за допомогою `bind`:
```js
setTimeout(user.sayHi.bind(user), 1000); // ✅ "Привіт, Марія"
```

---

## 7. 🧩 Практичні приклади

### 7.1. Використання `call` для наслідування
```js
function Animal(name) {
  this.name = name;
}

function Dog(name, breed) {
  Animal.call(this, name);
  this.breed = breed;
}

const dog = new Dog("Бім", "Лабрадор");
console.log(dog.name); // "Бім"
```

### 7.2. Копіювання методу іншого об’єкта
```js
const person1 = { name: "Юля", sayName() { console.log(this.name); } };
const person2 = { name: "Ігор" };

person1.sayName.call(person2); // "Ігор"
```

### 7.3. Передача `this` у колбеки
```js
const counter = {
  count: 0,
  inc() {
    this.count++;
    console.log(this.count);
  }
};

document.body.addEventListener("click", counter.inc.bind(counter));
// Натискання по сторінці кожного разу збільшує count
```

---

## 8. ⚖️ Порівняння `call`, `apply`, `bind`

| Метод | Викликає відразу? | Як передаються аргументи | Результат |
|--------|--------------------|---------------------------|------------|
| **call** | ✅ Так | Список аргументів | Значення функції |
| **apply** | ✅ Так | Масив аргументів | Значення функції |
| **bind** | ❌ Ні | Список аргументів | Нова функція із фіксованим контекстом |

---

## 9. 🧠 Типові помилки

| Ситуація | Проблема |
|-----------|-----------|
| Передача методу як колбеку (`setTimeout(obj.method, 1000)`) | Втрачається `this` |
| Застосування стрілкових функцій як методів | Не мають власного `this` |
| Забудьте `new` з конструктором | `this` = глобальний об’єкт або `undefined` |

---

## 🧭 Коротке резюме

| Концепт | Пояснення |
|----------|------------|
| **this** | Поточний контекст виконання |
| **Context** | Об’єкт, на який посилається `this` |
| **call / apply** | Викликають функцію з явно заданим контекстом |
| **bind** | Створює нову функцію зі сталим контекстом |
| **Arrow Function** | Не має власного `this`, бере з лексичного оточення |

---

## 📚 Додаткові матеріали

- MDN: [this](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this)
- MDN: [Function.prototype.bind()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind)
- MDN: [call()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/call)
- MDN: [apply()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/apply)
- JavaScript.info: [Контекст виклику, this](https://uk.javascript.info/object-methods)

---

## 🧩 Висновок

Ключове слово `this` у JavaScript — це незначна дрібниця, яка визначає поведінку **всього контексту виконання**.
Розуміння `this`, `bind`, `call`, `apply` допоможе:
- уникати втрати контексту у колбеках,
- глибше зрозуміти об’єктно-орієнтований JS,
- легше працювати з компонентами та подіями в React, Node.js та інших середовищах.

---
