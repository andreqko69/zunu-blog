# 📝 Todo App - React (CDN версія)

Повнофункціональний React todo додаток, який **не потребує npm, Node.js або будь-яких інструментів для збірки!**

## 🚀 Швидкий Старт

### Просто відкрийте файл!

```bash
# Це все що вам потрібно:
open index.html

# Або перетягніть файл в браузер
```

**Це працює! Жодних встановлень, жодних залежностей, жодного `npm install`!**

## 🎯 Що це?

Це React todo додаток побудований на:
- **React 18** з CDN (unpkg)
- **Babel Standalone** для JSX трансформації
- **Чистий HTML/CSS/JS** в одному файлі
- **Абсолютно жодних залежностей**

## ✨ Особливості

✅ Додавання нових справ  
✅ Позначення як виконане/невиконане  
✅ Видалення справ  
✅ Фільтрація (Всі, Активні, Виконані)  
✅ Статистика справ  
✅ Збереження в localStorage  
✅ Адаптивний дизайн  
✅ React Hooks (useState, useEffect)  
✅ Компонентна архітектура  

## 🏗️ Архітектура

Код містить декілька функціональних компонентів React:

```
App (головний компонент)
├── TodoInput (введення справи)
├── FilterButtons (кнопки фільтрів)
├── TodoStats (статистика)
├── TodoList (список справ)
│   └── TodoItem (окрема справа)
└── Actions (кнопки дій)
```

## 💾 Як це працює

### Завантаження React з CDN

```html
<!-- React та ReactDOM -->
<script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
<script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>

<!-- Babel для JSX -->
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
```

### JSX у браузері

```html
<script type="text/babel">
    // Ваш React код тут
    function App() {
        return <h1>Привіт, React!</h1>;
    }
    
    ReactDOM.createRoot(document.getElementById('root')).render(<App />);
</script>
```

## 🆚 Порівняння з npm версією

| Характеристика | CDN версія | npm версія |
|---|---|---|
| **Запуск** | Одразу ✅ | npm install + npm start |
| **Файлів** | 1 HTML файл | 10+ файлів |
| **Залежностей** | Нема | 3+ пакети |
| **Розмір** | ~50kb HTML | ~100kb+ після build |
| **Для навчання** | Ідеально! | Production-ready |
| **Перформанс** | Добре | Краще (з оптимізацією) |

## 📱 Браузерна Підтримка

✅ Chrome  
✅ Firefox  
✅ Safari  
✅ Edge  
✅ Мобільні браузери  

## 🔧 Редагування та Розширення

### Додавання нового компонента

```jsx
function MyNewComponent({ props }) {
    return <div>Новий компонент</div>;
}
```

### Використання в App

```jsx
function App() {
    return (
        <div>
            <MyNewComponent />
        </div>
    );
}
```

### Додавання нового стану

```jsx
const [myState, setMyState] = useState(initialValue);
```

### Додавання побічного ефекту

```jsx
useEffect(() => {
    // Код тут виконується при зміні залежностей
}, [dependency]);
```

## 📊 Управління Станом

Весь стан управляється в компоненті `App`:

```javascript
const [todos, setTodos] = useState([]);
const [currentFilter, setCurrentFilter] = useState('all');

// Завантажити з localStorage
useEffect(() => {
    const saved = localStorage.getItem('todos');
    if (saved) setTodos(JSON.parse(saved));
}, []);

// Зберегти до localStorage
useEffect(() => {
    localStorage.setItem('todos', JSON.stringify(todos));
}, [todos]);
```

## 💡 Переваги Цього Підходу

### Для Навчання
- 👍 Видно весь код в одному файлі
- 👍 Нема магії build процесу
- 👍 Легко експериментувати
- 👍 Легко розуміти як React працює

### Для Быстрого Прототипування
- 👍 Жодних встановлень
- 👍 Просто відкрийте та працюйте
- 👍 Можна поділитися посиланням на CodePen/JSFiddle
- 👍 Мобільна розробка (відкрийте на телефоні)

### Обмеження
- ❌ Babel преобразує JSX при кожному завантаженні (повільно на production)
- ❌ Нема build оптимізації
- ❌ Складніше розміщувати на сервері
- ❌ Для великих проектів краще npm версія

## 🚀 Коли Використовувати CDN Версію

✅ Навчання React  
✅ Швидкий прототип  
✅ Демонстрація ідей  
✅ Простий додаток  
✅ Локальна розробка  

❌ Production додаток  
❌ Великий проект  
❌ Потребує оптимізації  
❌ Team розробка  

## 🔗 Використовані CDN

- **React 18** - https://unpkg.com/react@18/
- **ReactDOM 18** - https://unpkg.com/react-dom@18/
- **Babel Standalone** - https://unpkg.com/@babel/standalone/

## 📚 Корисні Ресурси

- [React Docs](https://react.dev)
- [Babel Try It Out](https://babeljs.io/repl)
- [unpkg CDN](https://unpkg.com/)
- [React Hooks API](https://react.dev/reference/react/hooks)

## 🎓 Навчальні Завдання

### Рівень 1: Новачок

1. Змініть заголовок на `<h1>Мої улюблені справи</h1>`
2. Змініть колір кнопки
3. Додайте новий фільтр "Важливі"

### Рівень 2: Середній

1. Додайте приоритет справам (high, medium, low)
2. Сортуйте за пріоритетом
3. Покажіть число справ для кожного пріоритету

### Рівень 3: Експерт

1. Додайте дати виконання
2. Сортуйте за датами
3. Покажіть справи що прострочені
4. Додайте нотифікації

## 📖 Структура Файлу

```
index.html (616 рядків)
├── <head>
│   ├── <meta>
│   ├── <title>
│   └── <style> (CSS)
└── <body>
    ├── <div id="root">
    ├── <script> React з CDN
    ├── <script> ReactDOM з CDN
    ├── <script> Babel
    └── <script type="text/babel"> (JSX код)
        ├── TodoInput компонент
        ├── TodoItem компонент
        ├── TodoList компонент
        ├── FilterButtons компонент
        ├── TodoStats компонент
        └── App компонент
```

## 🎯 Приклади Видозмін

### Приклад 1: Темна Тема

```jsx
// Додайте стан для теми
const [isDark, setIsDark] = useState(false);

// Застосуйте клас
<div className={isDark ? 'dark' : 'light'}>
    {/* контент */}
</div>

// Додайте CSS
const darkStyles = `
    body.dark {
        background: #1a1a1a;
        color: #fff;
    }
`;
```

### Приклад 2: Пошук

```jsx
const [search, setSearch] = useState('');

const filteredBySearch = filteredTodos.filter(todo =>
    todo.text.toLowerCase().includes(search.toLowerCase())
);
```

### Приклад 3: Таймер для Справ

```jsx
// Додайте дедлайн до структури todo
const newTodo = {
    id: Date.now(),
    text: text,
    completed: false,
    deadline: new Date().getTime() + 24 * 60 * 60 * 1000, // завтра
};
```

## 🐛 Типові Проблеми

### JSX не компілюється

**Проблема:** `Unexpected token '<'`

**Рішення:** Переконайтесь що скрипт має `type="text/babel"`

```html
<!-- ❌ Неправильно -->
<script>
    function App() { return <div>Test</div>; }
</script>

<!-- ✅ Правильно -->
<script type="text/babel">
    function App() { return <div>Test</div>; }
</script>
```

### React не завантажується

**Проблема:** Помилка у консолі про React undefined

**Рішення:** Переконайтесь що скрипти завантажені по порядку

```html
<!-- Порядок важливий! -->
<script src="...react.js"></script>
<script src="...react-dom.js"></script>
<script src="...babel.js"></script>
<script type="text/babel">/* Ваш код */</script>
```

### localStorage не працює

**Проблема:** localStorage піддає помилку при файловому протоколі

**Рішення:** Запустіть локальний сервер

```bash
# Python 3
python -m http.server 8000

# Node.js
npx http-server

# Перейдіть на http://localhost:8000
```

## 🚀 Розвиток

### Наступний Крок: npm Версія

Коли освоїтесь з React, переходьте на npm версію для:
- Build оптимізації
- Розділу на файли
- Production готовності

### Потім Вивчайте

- React Router для навігації
- Redux для управління станом
- Next.js для full-stack
- TypeScript для типів

## 📞 Підтримка

Якщо щось не працює:

1. Відкрийте DevTools (F12)
2. Перевірте Console tab для помилок
3. Перевірте Network tab чи CDN завантажено
4. Спробуйте локальний сервер
5. Гуглите помилку

## 📄 Ліцензія

Цей код вільний для використання та модифікації в навчальних цілях.

---

**Enjoy React без npm! 🚀**

Створено для навчання React базово на CDN.