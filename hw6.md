# 🧠 Домашнє завдання: JavaScript — Context (`this`, `bind`, `call`, `apply`)

## 📚 Завдання

---

### 🧩 Завдання 1 — Контекст у методах об'єкта

**Опис:**
Створи об'єкт `user` з полями `name` і `age` та методом `greet`, який виводить:
> Привіт, мене звати <ім'я> і мені <вік> років.

Потім передай метод у змінну, виклич його й виправ контекст за допомогою `bind`.

**Шаблон коду:**

```js
// Завдання 1

const user = {
  name: "",
  age: 0,
  greet() {
    // Ваш код тут
  },
};

// Створіть змінну greetUser
// Викличте greetUser()
// Використайте bind для виправлення контексту
```

---

### 🧩 Завдання 2 — Використання `call`

**Опис:**
Створи функцію `introduce` і виклич її з переданим контекстом через `call`.

**Шаблон коду:**

```js
// Завдання 2

function introduce() {
  // Ваш код тут
}

const person = {
  name: "",
  age: 0,
};

// Викличте introduce з контекстом person
```

---

### 🧩 Завдання 3 — Використання `apply`

**Опис:**
Створи функцію `showFullNameAndCity`, яка приймає `surname` і `city`.
Виклич її з контекстом `user` через `apply`.

**Шаблон коду:**

```js
// Завдання 3

function showFullNameAndCity(surname, city) {
  // Ваш код тут
}

const user = {
  name: "",
};

// Викличте showFullName через apply та передайте масив аргументів
```

---

### 🧩 Завдання 4 — `bind` для часткового застосування

**Опис:**
Створи функцію `multiply`, потім зроби копії `double` і `triple` з “попередньо заповненим” аргументом.

**Шаблон коду:**

```js
// Завдання 4

function multiply(a, b) {
  // Ваш код тут
}

// Створіть функції double та triple, які подвоюють і потроюють відповідно

// Перевірте роботу функцій у консоль
```

---

### 🧩 Завдання 5 — Контекст та `bind`

**Опис:**
Створи об'єкт `counter`, передай його метод у змінну без `bind`, потім виправ контекст.

**Шаблон коду:**

```js
// Завдання 5

const counter = {
  count: 0,
  increment() {
    // Ваш код тут
  },
  decrement() {
    // Ваш код тут
  },
  show() {
    // Ваш код тут
  },
};

// Викличте кілька разів increment()
// Передайте метод increment у змінну inc
// Викличте inc() двічі
// Виправте, якщо не працює
```

---
